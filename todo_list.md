### 一、修正优化问题的逻辑

1. 带宽需求待分解为上行和下行 --完成
2. 流的需求修改优化请求 --完成

### 二、完善两个智能体的输入输出

1. Itent 的输入应该是编码后的 UeContext 和 UeIntent ，而其输出字段是否是这样还需要考虑
2. Itent 的输入UeContext的具体字段和结构需要明确 UeSmPolicyContext，UeAmPolicyContext
3. Itent 的输出的具体字段和结构需要确认是否符合最终要求
4. Optimization 的输出字段定义  --完成
5. Optimization 使用工具获取网络状态  --完成

### 三、优化求解器

1. 切片分配依据
2. 流的关键性能指标（KPI）和服务质量（QoS）要求，例如时延、带宽、丢包率和可靠性。 --完成

### 三、结构化输出

### 四、完善 Policy Dispatch agent 的逻辑

- 如何给 pcf 发送policy --需要模拟
- 如何从 pcf 获取UeContext --模拟
- 如何从 MANO 获取节点状态
- ** 如何获取 切片/流 状态 **

### 五、添加 RAG 库

1. 添加额外专业知识 -- 完成（还需修改）
2. 存储过往成功策略（需要考虑如何构建这个数据结构）
3. 考虑成功策略的有效时间
4. 知识注入工具 -- 完成

### 六、确认模型训练所需数据


### 七、一些问题

1. 如何确保LLM的及时反馈和输出
2. Intent的输入是用户意图以及UeContext，在实际应用场景中，如何得到用户意图？？ -- 工单映射？
3. Intent的输入用户意图与UeContext如何对应起来？ - 使用supi
4. 切片资源是否有其他指标？除了上下行带宽和延迟，还有丢包率、抖动，也就是 SLA（服务等级协议） --完成
5. RAG 知识库还不够专业和规范
6. 生成的策略没有键合法性检测，容易生成多余符号
7. 没有真实反馈


重要！！三个智能体是否都使用 supi 标识用户？（肯定的）ursp并未使用supi标识，而是使用5元组，而且应该与 AMF 结合。

后续需要考虑如何保持智能体系统间的会话一致性标识。
free5gc 并未实现 UrspRules 以及相关逻辑，考虑是否需要自己实现。

是否可以从 网络监控数据/工单 映射 用户意图 ？

底层切片资源的分配应该如何融合？（目前使用：分配现有切片，若不满足条件，则在底层资源中再分配新切片



TODO: 将RAN与CN仿真实现，目前optimizer中增加了一个新的切片资源分配优化问题（待验证）
tools中新增了ran_scheduler，用于RAN侧的切片无线资源调度，实现参考的是一篇 TMC 26 的论文，可能有问题需要修改，暂时不接入agent中使用。

思考如何比较好的连接上 RAN 侧的切片分配和 CN 侧的流量分配
1、flow SLA 聚合成切片的 SLA ，CN侧指导flow的切片分配，而RAN-TN-CN负责实际切片资源落实（可能可以使用论文中的 RL 算法），较为困难但切合实际
2、直接连接 RAN 和 CN，重构优化问题，同时考虑 RAN 的无线资源和 CN 的计算资源（可以使用上上周论文中的建模），难度中等

需要重构一下项目的 tools/optimizer！！！！可以考虑删除ran_scheduler再让ai重写

RAG 知识库的构建需要下点功夫。(重要)

TODO
不重构优化问题，实现一个 RAN+TN+模拟pcf 的仿真器。仿真器运行切片与流并实时更新slice、flow的状态

数据重构！！需要重新设计一下flow、Uecontext

OSA通过查询网络状态，能做到什么呢？给出建议？



3.23 TODO
OSA测试再次测试一下，可以考虑修改获取网络剪影的大小，以及optimizer返回的报告的大小  --完成
PDA需要和仿真器进行连接测试
闭环验证


4.3 TODO
RAG库解析PDF的逻辑升级
知识查询升级

数据集构建思考

agentic rl 文章阅读


4.6 TODO
1. 思考ns-3的作用，查询一些文章
2. 完善工具调用评价工具
3. 

4.10 TODO
1. 思考ns-3的作用
2. 完善工具调用评价工具
3. 寻找多智能体协作的文章
4. 一带多？成本上太大了
5. 试试skill，skill 是比 function calling 更细致的东西，类似于将领域知识注入上下文；这个很不错，但是好像比较适合多轮对话的agent，而不是离线协作的agent；
6. 还是要思考一下数据集构建的方法
7. 引入移动性是对的，单纯的 QoS 控制，成为流水线是无可避免的，需要引入更多责任、更多工具才可以体现多智能体系统的价值；
8. 



vmrun -T ws start "D:\VMware_Ubuntu\Ubuntu.vmx" nogui
4.14 TODO
1. 尝试使用 free5gc + UERANSIM + ns-3
2. 思考优化问题的优化
3. 数据集收集需要优化
4. RAG 库的不精确问题需要解决 --解决
